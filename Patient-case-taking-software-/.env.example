# Frontend (Vite) environment template
# ONLY VITE_-prefixed variables are included in the browser bundle.
# DO NOT add any AI provider secrets here.
# AI provider keys (Gemini, Groq) belong in the ROOT-level .env and are read only by the Node server.

# === NO SECRETS BELOW THIS LINE ===
# Add only non-sensitive Vite config values here if needed.
